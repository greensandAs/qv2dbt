# QlikView -> dbt / Snowflake Migration Report

**Source script:** `stress_test.qvs`  
**Generated by:** qv2dbt accelerator

## 1. Summary

| Metric | Value |
|---|---|
| QlikView tables | 4 |
| dbt models generated | 4 |
| Mapping tables | 1 |
| External sources (QVD/file/SQL) | 2 |
| Script variables | 2 |
| Total fields | 29 |
| Fields needing review | 2 |
| **Auto-translatable** | **93.1%** |
| Unsupported statements | 10 |

## 2. dbt Layer Assignment

**staging** (1): `Customers`
**intermediate** (2): `Orders`, `Orders__join3`
**mart** (1): `sales_fact`

## 3. Table Inventory

| QlikView table | Layer | Load kind | Source | Fields | Joins | Review |
|---|---|---|---|---|---|---|
| Customers | staging | qvd | `lib://DW/qvd/customers.qvd` | 9 | - | 0 |
| Orders | intermediate | qvd | `lib://DW/qvd/orders.qvd` | 9 | Orders__join3 | 0 |
| Orders__join3 | intermediate | resident | `Customers` | 3 | - | 0 |
| sales_fact | mart | resident | `Orders` | 8 | - | 1 |

## 4. Field-Level Lineage

QlikView expression -> Snowflake SQL (per target field).


### Customers

| Target field | QlikView expression | Snowflake SQL | Review |
|---|---|---|---|
| CustomerID | `CustomerID` | `CustomerID` |  |
| CustomerName | `Capitalize(CustomerName)` | `INITCAP(CustomerName)` |  |
| Email | `Upper(Trim(Email))` | `UPPER(TRIM(Email))` |  |
| Country | `ApplyMap('CountryMap', CountryCode, 'N/A')` | `{{ apply_map('CountryMap', "CountryCode", "'N/A'") }}` | yes |
| AreaCode | `Left(Phone, 3)` | `LEFT(Phone, 3)` |  |
| CreditLimit | `if(IsNull(CreditLimit), 0, CreditLimit)` | `CASE WHEN CreditLimit IS NULL THEN 0 ELSE CreditLimit END` |  |
| SignupDate | `Date(SignupDate, 'YYYY-MM-DD')` | `TO_DATE(SignupDate, 'YYYY-MM-DD')` |  |
| RenewalDate | `AddYears(SignupDate, 1)` | `DATEADD('YEAR', 1, SignupDate)` |  |
| BalanceSign | `Sign(Balance)` | `SIGN(Balance)` |  |

### Orders

| Target field | QlikView expression | Snowflake SQL | Review |
|---|---|---|---|
| OrderID | `OrderID` | `OrderID` |  |
| CustomerID | `CustomerID` | `CustomerID` |  |
| ProductID | `ProductID` | `ProductID` |  |
| Quantity | `Num(Quantity)` | `TO_NUMBER(Quantity)` |  |
| GrossAmount | `Num(UnitPrice) * Num(Quantity)` | `TO_NUMBER(UnitPrice) * TO_NUMBER(Quantity)` |  |
| MaxDiscount | `RangeMax(Discount1, Discount2)` | `GREATEST(Discount1, Discount2)` |  |
| PrevOrderRef | `Peek('PrevOrder', -1, 'Orders')` | `/* TODO review */ Peek('PrevOrder', -1, 'Orders')` | yes |
| OrderDate | `Date(OrderDate)` | `TO_DATE(OrderDate)` |  |
| OrderMonth | `MonthStart(OrderDate)` | `DATE_TRUNC('MONTH', OrderDate)` |  |

### Orders__join3

| Target field | QlikView expression | Snowflake SQL | Review |
|---|---|---|---|
| CustomerID | `CustomerID` | `CustomerID` |  |
| Country | `Country` | `Country` |  |
| CreditLimit | `CreditLimit` | `CreditLimit` |  |

### sales_fact

| Target field | QlikView expression | Snowflake SQL | Review |
|---|---|---|---|
| ProductID | `ProductID` | `ProductID` |  |
| Country | `Country` | `Country` |  |
| OrderYear | `Year(OrderDate)` | `YEAR(OrderDate)` |  |
| OrderMonth | `Month(OrderDate)` | `MONTH(OrderDate)` |  |
| TotalRevenue | `Sum(GrossAmount)` | `SUM(GrossAmount)` |  |
| OrderCount | `Count(OrderID)` | `COUNT(OrderID)` |  |
| AvgOrderValue | `Avg(GrossAmount)` | `AVG(GrossAmount)` |  |
| MedianOrderValue | `Median(GrossAmount)` | `MEDIAN(GrossAmount)` |  |

## 5. Items Requiring Manual Review

| Table | Field | Note |
|---|---|---|
| Customers | Country | ApplyMap('CountryMap', ...) converted to apply_map() macro - confirm mapping table was generated. |
| Orders | PrevOrderRef | Function 'Peek()' requires manual review (no deterministic Snowflake equivalent). |

## 6. Unsupported / Manually-Handled Statements

These QlikView statements have no direct dbt equivalent and were recorded for manual handling (control flow, STORE, RENAME, DROP):

| Kind | Statement (truncated) |
|---|---|
| control | `SUB LoadQVD(tableName, fileName)     [$(tableName)]:     LOAD * FROM [lib://DW/q` |
| control | `END SUB  CALL LoadQVD('RawCustomers', 'customers.qvd')` |
| control | `FOR EACH vFile IN 'sales_2023.qvd', 'sales_2024.qvd'     SalesHist:     LOAD Ord` |
| control | `NEXT vFile   IF WeekDay(vRunDate) = 0 THEN     TRACE Running Monday full load` |
| store | `STORE sales_fact INTO [lib://DW/qvd/sales_fact.qvd] (qvd)` |
| drop | `DROP TABLE Orders` |
| control:sub | `SUB LoadQVD(tableName, fileName)` |
| control:call | `CALL LoadQVD('RawCustomers', 'customers.qvd')` |
| control:for | `FOR EACH vFile IN 'sales_2023.qvd', 'sales_2024.qvd'` |
| control:if | `IF WeekDay(vRunDate) = 0 THEN` |

**Dropped tables (QlikView DROP):** Orders

## 7. Construct Mapping Legend

| QlikView | dbt / Snowflake target |
|---|---|
| `LOAD ... FROM x.qvd` | staging model + `source()` + RAW DDL |
| `LOAD ... RESIDENT` | intermediate model + `ref()` |
| `LEFT/INNER JOIN` | SQL `JOIN ... USING(common cols)` |
| `CONCATENATE` | `UNION ALL` |
| `MAPPING LOAD` + `ApplyMap()` | mapping model + `apply_map()` macro |
| `if()/Date()/Num()/&` | `CASE`/`TO_DATE`/`TO_NUMBER`/`||` |
| `STORE/DROP/RENAME` | handled by dbt materialization (not migrated) |
