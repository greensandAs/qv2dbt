version: 2

sources:
  - name: qlikview_raw
    database: LUNDBECK_UKIE
    schema: raw
    description: >
      Raw QlikView-sourced tables (QVD/file/SQL extracts) landed in Snowflake.
    tables:
      - name: customers
        description: 'Origin: lib://DW/qvd/customers.qvd'
      - name: orders
        description: 'Origin: lib://DW/qvd/orders.qvd'
