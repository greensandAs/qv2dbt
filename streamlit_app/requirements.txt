# qv2dbt Studio (Streamlit app)
streamlit>=1.31
pandas>=2.0
PyYAML>=6.0
openpyxl>=3.1

# Snowflake + Cortex (not needed for parsing/conversion; required for the
# Snowflake connection, Cortex chatbot, AI suggestions, and "Run in Snowflake").
# In Streamlit-in-Snowflake these are already present.
snowflake-snowpark-python>=1.14
snowflake-connector-python>=3.7
snowflake>=0.8          # snowflake.core, for Cortex Search service API
